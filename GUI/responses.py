def block_ip(ip):
    """Block a given IP address"""
    print(f"Blocking IP: {ip}")
    # Add logic to block the IP (e.g., update firewall rules)

def unblock_ip(ip):
    """Unblock a given IP address"""
    print(f"Unblocking IP: {ip}")
    # Add logic to unblock the IP (e.g., remove from firewall rules)

def send_reset_packets(ip):
    """Send TCP reset packets to terminate connections with the given IP"""
    print(f"Sending TCP reset packets to {ip}")
    # Add logic to send TCP reset packets
